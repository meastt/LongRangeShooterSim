name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install
        run: npm ci

      # Build before anything that imports workspace packages — their types
      # resolve to dist/, so a fresh checkout fails app typecheck without this.
      - name: Solver build
        run: npm run build -w @aim/solver

      - name: Bullet library build
        run: npm run build -w @aim/bullet-library

      - name: Solver tests
        run: npm run test:solver

      - name: Bullet library tests
        run: npm run test -w @aim/bullet-library

      - name: BLE parser tests
        run: npm run test:ble

      - name: App lib tests (importers, QR, profile mapping)
        run: npm run test:app-lib

      - name: App typecheck
        run: npx tsc --noEmit
        working-directory: app
